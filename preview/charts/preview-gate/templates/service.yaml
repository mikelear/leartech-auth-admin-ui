apiVersion: v1
kind: Service
metadata:
  name: preview-gate
  labels:
    app: preview-gate
spec:
  type: ClusterIP
  ports:
  - port: 8090
    targetPort: 8090
    protocol: TCP
    name: http
  selector:
    app: preview-gate
