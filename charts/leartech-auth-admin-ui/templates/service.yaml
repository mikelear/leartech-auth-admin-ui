apiVersion: v1
kind: Service
metadata:
  name: {{ include "leartech-auth-admin-ui.fullname" . }}
  labels:
    {{- include "leartech-auth-admin-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leartech-auth-admin-ui.selectorLabels" . | nindent 4 }}
