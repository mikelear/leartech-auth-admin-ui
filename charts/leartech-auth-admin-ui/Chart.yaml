apiVersion: v2
name: leartech-auth-admin-ui
description: Leartech Auth Admin UI — Angular SPA for tenant, OAuth client, user, and audit-log management via leartech-auth-service /admin/* routes. Two-mode deployment (platform / tenant) via Helm values.
version: 0.0.4
appVersion: 0.0.4
type: application
